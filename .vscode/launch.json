{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C/C++: gcc 디버그",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main_static",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "build" 
        }
    ]
}